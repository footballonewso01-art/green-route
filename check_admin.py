
import urllib.request
import json
import os

admin_email = os.environ.get('PB_ADMIN_EMAIL', 'admin@example.com') # I don't know it, let me just run a script on Fly to get an admin token or query via SQLite.

